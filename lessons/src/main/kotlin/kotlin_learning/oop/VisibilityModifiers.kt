package kotlin_learning.oop

// final (used by default): cannot be overridden
// open: can be overridden (should be marked explicitly)
// abstract: must be overridden (cannot have implementation)
// override(mandatory): overrides a member in superclass or interface

/**
 * Modifier       calss member           top-level declaration
 * public             visible     everywhere
 * internal           visible in the module
 * protected      visible in subclass    ---------------------
 * private        visible in the class   visible in the file
 **/